dist: bionic
group: travis_latest

os: linux
language: python

matrix:
  include:
    - env: CXX=g++

before_install:
  - sudo apt-get -qq update
  - sudo apt-get install libpulse-dev python3-pip python3-setuptools cmake g++-9 clang-9 libstdc++6

install:
  - sudo python3 -m pip install meson ninja
  - git clone https://github.com/SCOTT-HAMILTON/FakeLib
  - cd FakeLib
  - mkdir build
  - g++ --version
  - clang --version
  - meson build
  - ninja -C build
  - sudo ninja -C build install
  - cd ..
  - git clone https://github.com/p-ranav/argparse
  - cd argparse
  - mkdir build
  - cd build
  - cmake -G Ninja -DCMAKE_BUILD_TYPE=Release -DARGPARSE_BUILD_TESTS=ON -DCMAKE_CXX_COMPILER=g++ ..
  - cd ..
  - ninja -C build
  - sudo ninja -C build install
  - cd ..

script:
  - mkdir build
  - meson build
  - ninja -C build

